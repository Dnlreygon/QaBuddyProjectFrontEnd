name: Node.js CI

on:
  push:
    branches: [ master , development ]
  pull_request:
    branches: [ master , development ]
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron: '0 */12 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
      env:
        CI: true
        EMAIL: dnlreygon34@gmail.com
        PASSWORD: contraseña123
        WRONGPASSWORD: wrong
        EMPTYPWD: 
        WRONGEMAIL: blabla
        BROWSERSTACK_USERNAME: eduardo.contreras@wizeline.com
        BROWSERSTACK_ACCESS_KEY: cRnaDyBopbq2EVKMdzrk
        SAUCE_USERNAME: georgevx
        SAUCE_ACCESS_KEY: dbb85f44-7514-4004-b952-b3ac248d0dbc











